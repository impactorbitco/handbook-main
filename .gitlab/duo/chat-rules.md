# Development guide

## General style guide

## Markdown style guide

- Always add a new line between headings and paragraphs.

## CI/CD Configuration

- Do not use YAML anchors but instead job extends.
- Always use rules in jobs, `only` is not allowed.
